package rebalance

func getUsersByActiveStrategy(strategyId string) {
	// gets all users with an active strategy of a specific strategyId from ActiveStrategies table
	// returns a list of userId, amount, and alpacaId
}
