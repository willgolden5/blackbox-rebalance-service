package rebalance

import (
	"fmt"
)

// call this in a goRoutine
func RebalanceUserPortfolios(strategyId string) {
	// get users where active strategyId = strategyId
	fmt.Println("rebalance all portfolios hit")
}
